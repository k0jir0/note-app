apiVersion: v1
kind: Namespace
metadata:
  name: note-app
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: note-app-runtime
  namespace: note-app
data:
  NODE_ENV: production
  PORT: "3000"
  MONGODB_URI: mongodb://note-app-mongo:27017/noteApp
  DISABLE_KEYTAR: "1"
  DISABLE_REDIS: "1"
  ENABLE_REALTIME: "0"
  LOG_BATCH_ENABLED: "false"
  SCAN_BATCH_ENABLED: "false"
  INTRUSION_BATCH_ENABLED: "false"
  TRUST_PROXY_HOPS: "1"
  APP_BASE_URL: http://localhost:3002
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: note-app-proxy-config
  namespace: note-app
data:
  default.conf: |
    client_body_temp_path /tmp/client_temp;
    proxy_temp_path /tmp/proxy_temp;
    fastcgi_temp_path /tmp/fastcgi_temp;
    uwsgi_temp_path /tmp/uwsgi_temp;
    scgi_temp_path /tmp/scgi_temp;

    upstream note_app_backend {
        server note-app:3000;
        keepalive 16;
    }

    server {
        listen 8080;
        server_name _;
        server_tokens off;

        client_max_body_size 10m;

        location = /healthz {
            access_log off;
            add_header Content-Type text/plain;
            return 200 'ok';
        }

        location /api/security/stream {
            proxy_pass http://note_app_backend;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Port $server_port;
            proxy_set_header Connection '';
            proxy_buffering off;
            proxy_read_timeout 3600s;
        }

        location / {
            proxy_pass http://note_app_backend;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Port $server_port;
            proxy_set_header Connection '';
            proxy_redirect off;
        }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: note-app-mongo
  namespace: note-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: note-app
      app.kubernetes.io/component: mongo
  template:
    metadata:
      labels:
        app.kubernetes.io/name: note-app
        app.kubernetes.io/component: mongo
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: mongo
          image: mongo:7
          imagePullPolicy: IfNotPresent
          command:
            - mongod
            - --bind_ip_all
          ports:
            - containerPort: 27017
              name: mongo
          readinessProbe:
            tcpSocket:
              port: mongo
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            tcpSocket:
              port: mongo
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
          volumeMounts:
            - name: mongo-data
              mountPath: /data/db
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      volumes:
        - name: mongo-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: note-app-mongo
  namespace: note-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: note-app
    app.kubernetes.io/component: mongo
  ports:
    - name: mongo
      port: 27017
      targetPort: mongo
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: note-app
  namespace: note-app
spec:
  replicas: 3
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: note-app
      app.kubernetes.io/component: app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: note-app
        app.kubernetes.io/component: app
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: app
          image: note-app:hardened
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            - secretRef:
                name: note-app-secrets
            - configMapRef:
                name: note-app-runtime
          readinessProbe:
            httpGet:
              path: /auth/login
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /auth/login
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: note-app
  namespace: note-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: note-app
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 3000
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: note-app-proxy
  namespace: note-app
spec:
  replicas: 2
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: note-app
      app.kubernetes.io/component: proxy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: note-app
        app.kubernetes.io/component: proxy
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: proxy
          image: nginx:1.27-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
          volumeMounts:
            - name: proxy-config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
              readOnly: true
            - name: proxy-tmp
              mountPath: /tmp
            - name: proxy-cache
              mountPath: /var/cache/nginx
            - name: proxy-run
              mountPath: /var/run
          securityContext:
            runAsUser: 101
            runAsGroup: 101
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      volumes:
        - name: proxy-config
          configMap:
            name: note-app-proxy-config
        - name: proxy-tmp
          emptyDir: {}
        - name: proxy-cache
          emptyDir: {}
        - name: proxy-run
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: note-app-proxy
  namespace: note-app
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: note-app
    app.kubernetes.io/component: proxy
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: note-app-default-deny-ingress
  namespace: note-app
spec:
  podSelector: {}
  policyTypes:
    - Ingress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: note-app-allow-proxy-ingress
  namespace: note-app
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: note-app
      app.kubernetes.io/component: proxy
  policyTypes:
    - Ingress
  ingress:
    - {}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: note-app-allow-app-from-proxy
  namespace: note-app
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: note-app
      app.kubernetes.io/component: app
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: note-app
              app.kubernetes.io/component: proxy
      ports:
        - protocol: TCP
          port: 3000
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: note-app-rollout
  namespace: note-app
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: note-app-rollout
  namespace: note-app
rules:
  - apiGroups:
      - apps
    resources:
      - deployments
    verbs:
      - get
      - list
      - patch
  - apiGroups:
      - apps
    resources:
      - deployments/status
    verbs:
      - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: note-app-rollout
  namespace: note-app
subjects:
  - kind: ServiceAccount
    name: note-app-rollout
    namespace: note-app
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: note-app-rollout
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: note-app-allow-mongo-from-app
  namespace: note-app
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: note-app
      app.kubernetes.io/component: mongo
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: note-app
              app.kubernetes.io/component: app
      ports:
        - protocol: TCP
          port: 27017
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: note-app-daily-rollout
  namespace: note-app
spec:
  schedule: "0 3 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 3600
      template:
        spec:
          serviceAccountName: note-app-rollout
          restartPolicy: OnFailure
          containers:
            - name: kubectl
              image: bitnami/kubectl:1.32.3
              imagePullPolicy: IfNotPresent
              command:
                - /bin/sh
                - -c
                - |
                  kubectl rollout restart deployment/note-app deployment/note-app-proxy -n note-app
                  kubectl rollout status deployment/note-app -n note-app --timeout=10m
                  kubectl rollout status deployment/note-app-proxy -n note-app --timeout=10m